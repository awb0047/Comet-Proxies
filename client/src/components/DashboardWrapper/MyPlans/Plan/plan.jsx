import React from "react";
import { PlanWrapper, PlanName } from './planStyle'

export function Plan( {
    user, userData,
} ) {
    return (
        <>
            <PlanWrapper>
                <PlanName></PlanName>
            </PlanWrapper>
        </>
    );
}